apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: {{.APP_NAME}}
  namespace: {{.KUBE_NAMESPACE}}
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: {{.APP_NAME}}
        env: {{.KUBE_NAMESPACE}}
    spec:
      containers:
      - name: {{.APP_NAME}}
        image: "{{.IMAGE_TAG}}"
        imagePullPolicy: Always
        ports:
          - containerPort: 80
        resources:
          requests:
            memory: "0.05Gi"
            cpu: "0.008"
        readinessProbe:
          initialDelaySeconds: 10
          periodSeconds: 10
          failureThreshold: 60
          httpGet:
            port: 80
            path: /health
      imagePullSecrets:
      - name: {{.APP_NAME}}-registry-deploy-token
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0